type Query {
    books(author: String): [Book!]!
    bookById(id: ID!): Book
}

type Mutation {
    addBook(input: BookInput!): Book!
}

type Book {
    id: ID!
    title: String!
    author: String!
    year: Int
}

input BookInput {
    title: String!
    author: String!
    year: Int
}